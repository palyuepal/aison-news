# NutriFact Lab v0.9 production bundle

This branch is isolated from AIson main.

Vercel reconstructs the full v0.9 Next.js source from `source.tar.gz` during `prebuild` and then runs `next build` with Next.js 16.3.3.

Public-launch additions include favicon / Apple icon, Web App Manifest, generated Open Graph and Twitter images, a branded 404 page, and the v0.9 launch-status update.

Custom domain purchase, Search Console verification values, GA4 Measurement ID, official domain email and external professional reviewer remain intentionally unconfigured until real account values or explicit approval are available.
