name: Legacy AIson daily updater (disabled)

# AIson daily publication is handled by the Codex automation at 08:00 Hong Kong time.
# This workflow intentionally contains no API key, secret, scheduled trigger, or updater job.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — AIson is updated by the no-API Codex automation."
